---
description: Reconnect, switch accounts for, or repair scopes on an Omni provider (e.g. google, github, slack).
---

Use `Arcade_ManageToolAuthorization` to inspect and repair the authorization for
the provider or service below. Present the resulting authorization link to the
user with a one-line instruction, then stop — do not retry in a loop.

Provider / request:

$ARGUMENTS
